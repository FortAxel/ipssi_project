% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode$for(hyperrefoptions)$,$hyperrefoptions$$endfor$}{hyperref}
\PassOptionsToPackage{hyphens}{url}
$if(colorlinks)$
\PassOptionsToPackage{dvipsnames,svgnames,x11names}{xcolor}
$endif$

\documentclass[
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
]{$documentclass$}

%% ───── PACKAGES ─────────────────────────────────────────────────────────────
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{titlesec}
\usepackage{titling}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{setspace}
\usepackage{mdframed}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{fancyvrb}
\usepackage{float}
\usepackage{calc}
\usepackage{xfp}

%% ───── IMAGES ───────────────────────────────────────────────────────────────
% Limiter toutes les images à la largeur du texte, intégrées dans le flux
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>0.45\textheight 0.45\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Forcer les figures à rester dans le flux (pas de flottant)
\makeatletter
\def\fps@figure{H}
\makeatother

%% ───── POLICES ───────────────────────────────────────────────────────────────
$if(mainfont)$\setmainfont{$mainfont$}$endif$
$if(sansfont)$\setsansfont{$sansfont$}$endif$
$if(monofont)$\setmonofont[Scale=0.88]{$monofont$}$endif$

%% ───── GÉOMÉTRIE ─────────────────────────────────────────────────────────────
$if(geometry)$
\geometry{$geometry$}
$else$
\geometry{top=2.5cm, bottom=2.8cm, left=2.8cm, right=2.5cm}
$endif$

%% ───── COULEURS ──────────────────────────────────────────────────────────────
\definecolor{accentblue}{RGB}{41, 98, 178}
\definecolor{lightblue}{RGB}{235, 242, 252}
\definecolor{bordercolor}{RGB}{200, 215, 235}
\definecolor{codebg}{RGB}{245, 246, 247}
\definecolor{darktext}{RGB}{33, 37, 41}
\definecolor{mutedtext}{RGB}{108, 117, 125}

%% ───── HYPERREF ──────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=accentblue,
  pdftitle={$if(title)$$title$$endif$},
  pdfauthor={$if(author)$$author$$endif$},
}

%% ───── ESPACEMENT ────────────────────────────────────────────────────────────
$if(linestretch)$\setstretch{$linestretch$}$endif$
\setlength{\parskip}{0.6em}
\setlength{\parindent}{0pt}

%% ───── NUMÉROTATION DES TITRES ───────────────────────────────────────────────
% On gère la numérotation ici uniquement — ne pas mettre numbersections dans le .md ou metadata.yaml
$if(numbersections)$
  \setcounter{secnumdepth}{3}
$else$
  \setcounter{secnumdepth}{0}
$endif$

%% ───── STYLE DES TITRES ──────────────────────────────────────────────────────
\titleformat{\section}
  {\color{accentblue}\Large\bfseries}
  {\thesection}{1em}{}
  [\vspace{0.15em}{\color{bordercolor}\rule{\linewidth}{0.6pt}}\vspace{0.2em}]

\titleformat{\subsection}
  {\color{accentblue}\large\bfseries}
  {\thesubsection}{1em}{}

\titleformat{\subsubsection}
  {\color{accentblue}\normalsize\bfseries}
  {\thesubsubsection}{1em}{}

\titlespacing*{\section}{0pt}{1.6em}{0.6em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.4em}
\titlespacing*{\subsubsection}{0pt}{1em}{0.3em}

%% ───── EN-TÊTE / PIED DE PAGE ────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\fancyhead[L]{\small\color{accentblue}\textit{$if(title)$$title$$endif$}}
\fancyhead[R]{\small\color{darktext}\textit{$if(author)$$author$$endif$}}
\fancyfoot[L]{\small\color{mutedtext}\textit{$if(footer-left)$$footer-left$$else$IPSSI · CDA 2025-2026$endif$}}
\fancyfoot[C]{\small\color{darktext}Page \thepage\ / \pageref{LastPage}}
\fancyfoot[R]{\small\color{mutedtext}\textit{$if(date)$$date$$endif$}}
\setlength{\headheight}{16pt}
\setlength{\footskip}{20pt}

\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
  \fancyfoot[C]{\small\color{darktext}Page \thepage\ / \pageref{LastPage}}
  \fancyfoot[R]{\small\color{mutedtext}\textit{$if(date)$$date$$endif$}}
}

%% ───── CODE BLOCKS ───────────────────────────────────────────────────────────
\usepackage{framed}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

% Redéfinir Shaded (généré par Pandoc) : fond gris + bordure gauche bleue
\definecolor{shadecolor}{RGB}{245, 246, 247}
% Redéfinir Shaded : fonctionne qu'il soit défini ou non par highlighting-macros
\makeatletter
\@ifundefined{Shaded}{\newenvironment{Shaded}{}{}}{} % créer si absent
\makeatother
\renewenvironment{Shaded}{%
  \vspace{0.4em}%
  \def\FrameCommand{%
    {\color{accentblue}\vrule width 3pt}%
    \hspace{6pt}%
    \colorbox{shadecolor}%
  }%
  \MakeFramed{\advance\hsize-\width \FrameRestore}%
  \small
}{%
  \endMakeFramed%
  \vspace{0.4em}%
}

% Verbatim brut (sans coloration syntaxique) : même style
\RecustomVerbatimEnvironment{Verbatim}{Verbatim}{
  fontsize=\small,
}

%% ───── TABLEAUX ──────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.3}
\setlength{\tabcolsep}{8pt}

%% ───── LISTES ────────────────────────────────────────────────────────────────
\setlist[itemize]{itemsep=0.2em, topsep=0.4em}
\setlist[enumerate]{itemsep=0.2em, topsep=0.4em}
% Macro requise par Pandoc
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%% ───── LANGUE ────────────────────────────────────────────────────────────────
\usepackage{polyglossia}
\setmainlanguage{french}

%% ═════════════════════════════════════════════════════════════════════════════
\begin{document}

%% ───── PAGE DE TITRE ─────────────────────────────────────────────────────────
\begin{titlepage}
  \thispagestyle{empty}
  \begin{center}
    \vspace*{2cm}
    {\color{accentblue}\rule{\linewidth}{3pt}}
    \vspace{0.6cm}

    {\fontsize{24}{30}\selectfont\bfseries\color{accentblue}
      $if(title)$$title$$endif$
    }
    \vspace{0.4cm}

    $if(subtitle)$
    {\large\color{mutedtext}\textit{$subtitle$}}
    \vspace{0.3cm}
    $endif$

    {\color{accentblue}\rule{\linewidth}{1pt}}
    \vspace{2.5cm}

    \begin{mdframed}[
      linecolor=bordercolor,
      backgroundcolor=lightblue,
      linewidth=1pt,
      innertopmargin=16pt,
      innerbottommargin=16pt,
      innerleftmargin=20pt,
      innerrightmargin=20pt,
    ]
    \begin{center}
      \begin{tabular}{rl}
        \textbf{\color{accentblue}Étudiant}  & $if(author)$$author$$endif$ \\[4pt]
        \textbf{\color{accentblue}Formation} & IPSSI — CDA Bachelor Fullstack DevOps \\[4pt]
        \textbf{\color{accentblue}Promotion} & 2025–2026 \\[4pt]
        \textbf{\color{accentblue}Date}      & $if(date)$$date$$endif$ \\
      \end{tabular}
    \end{center}
    \end{mdframed}

    \vfill
    {\color{accentblue}\rule{\linewidth}{3pt}}
    \vspace{0.3cm}
    {\small\color{mutedtext}$if(project)$$project$$else$Application Web de Lecture d'Histoires pour Enfants — Projet Fil Rouge CDA$endif$}
  \end{center}
\end{titlepage}

%% ───── TABLE DES MATIÈRES ────────────────────────────────────────────────────
$if(toc)$
\newpage
{
  \hypersetup{linkcolor=black}
  \setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
  $if(toc-title)$\renewcommand*\contentsname{$toc-title$}$endif$
  \tableofcontents
}
\newpage
$endif$

%% ───── CORPS ─────────────────────────────────────────────────────────────────
$body$

\end{document}